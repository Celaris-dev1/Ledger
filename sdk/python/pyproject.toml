[project]
name = "ledger-sdk"
version = "0.1.0"
description = "Stdlib-only client for the Ledger tamper-evident agent decision log"
requires-python = ">=3.9"
license = {text = "Apache-2.0"}

[tool.setuptools]
packages = ["ledger_sdk"]
